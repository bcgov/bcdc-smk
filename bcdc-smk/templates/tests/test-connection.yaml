apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bcdc-smk.fullname" . }}-test-connection"
  labels:
    {{- include "bcdc-smk.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bcdc-smk.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
